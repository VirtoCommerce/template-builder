export interface EditorUIState {
    // mode: 'list' | 'tile';
    // presetsFilter: string | null;
};

export const initialState: EditorUIState = {
    // mode: 'list',
    // presetsFilter: null
};
