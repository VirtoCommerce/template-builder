import { createAction, props } from "@ngrx/store";

export * from './integrations';
export * from './presets';
export * from './theme';

// note: write here common actions

// export const empty = createAction('[Atms] empty');

// export const enterToModule = createAction('[Atms] enter to module', props<{ item: SitemapItem }>());
// export const enterToListMode = createAction('[Atms] enter to list mode', props<{ item: SitemapItem, mode: string }>());
// export const enterToMapMode = createAction('[Atms] enter to map mode', props<{ item: SitemapItem, mode: string }>());
// export const enterToDetailsMode = createAction('[Atms] enter to details mode', props<{ item: SitemapItem }>());

// export const applyModuleSettings = createAction('[Atms] apply module settings', props<{ settings: any }>());

